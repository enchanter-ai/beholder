/* @enchanter-ai/plugin-sylph — thin re-export shell.
   Canonical implementation lives in the root `beholder` package at
   src/plugins/sylph.adapter.ts. */

export { sylphAdapter } from 'beholder';
